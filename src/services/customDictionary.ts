// Contains a list of custom words that don't appear in the original dictionary

import { DictionaryWord } from "../models/dictionaryWord";

export const customDictionary: DictionaryWord[] = [
    {
        'en': 'cell phone',
        'fr': 'portable',
        'gen': 'm'
    }
]